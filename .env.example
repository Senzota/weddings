DATABASE_URL=postgres://user:password@localhost:5432/weddings103
SESSION_SECRET=change-me
PORT=3000

# Used only by `npm run db:seed-admin` to create/update the admin login
ADMIN_EMAIL=
ADMIN_PASSWORD=

# SMTP for gatepass emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Weddings 103 <no-reply@example.com>"

# Public base URL, used to build invite links and QR payloads
BASE_URL=http://localhost:3000
